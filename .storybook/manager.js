// .storybook/manager.js
import { addons } from '@storybook/addons';
import { themes } from '@storybook/theming';

addons.setConfig({
  theme: themes.dark                // Use Storybook’s built-in dark theme by default :contentReference[oaicite:11]{index=11}
});