// capacitor.config.ts

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.phantomar',
  appName: 'PhantomAR',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;